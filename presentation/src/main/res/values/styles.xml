<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--Components-->

    <!-- AppBarLayout -->
    <style name="ThemeOverlay.MyTheme.AppBarLayout" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorAccent">@color/white_semi_transparent</item>
        <item name="colorControlNormal">@color/white_semi_transparent</item>
        <item name="colorControlHighlight">@color/white</item>
        <item name="colorControlActivated">@color/white</item>
    </style>

    <style name="IconOnlyButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="iconPadding">0dp</item>
        <item name="iconGravity">textStart</item>
    </style>

</resources>
